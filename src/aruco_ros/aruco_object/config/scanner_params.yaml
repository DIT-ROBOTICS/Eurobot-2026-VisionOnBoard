aruco_row_scanner:
  ros__parameters:
    # Team color: "yellow" or "blue" (determines which IDs are targets)
    team_color: "yellow"
    
    # Target IDs (Placeholders, update with actual game rules)
    # IDs that trigger a "1" in the mask for the respective team
    # Note: ROS 2 params can be lists of integers
    blue_id: 36
    yellow_id: 47
    
    # Voting system: how many consistent frames needed
    votes_needed: 5
